### Window Manager Independant Keybindings ###

# description: Terminal Emulator # 
super + Return
	alacritty

# description: Dmenu Scripts # 
super + space ; {p,a,shift + p}
	{dmenu_run -c -l 15, dmenu_launch.py --apps, passmenu2}

# description: Open Applications
super + o ; {e,m,b,w}
    {st -e nvim,st -e neomutt,qutebrowser,ferdi}

# description: Reload SXHKD # 
super + ctrl + r
	pkill -USR1 -x sxhkd

## Notification Control Keybindings ##

# description: Clear Notifications
super + {_,shift + }c
    dunstctl {close,close-all}

## Audio Control ##

# description: Mute Volume
XF86AudioMute
    pamixer --toggle-mute

# description: Increase Volume by 10%
XF86AudioRaiseVolume
    pamixer -i 10

# description: Decrease Volume by 10%
XF86AudioLowerVolume
    pamixer -d 10

## XF86AudioPlay

## XF86AudioNext

## XF86AudioPrev

### BSPWM Hotkeys ###

# description: Restart BSPWM # 
super + ctrl + shift + r
	bspc wm -r
 
# description: Close or Kill Node # 
super + {d,x} 
	bspc node -{c,k}

## Window States & Flags ##

# description: Change Window Mode
super + w ; {t,shift + t,s,f}
	bspc node -t {tiled,pseudo_tiled,floating,fullscreen}

# description: Set Window Flags
super + w ; {m,x,y,z}
	bspc node -g {marked,locked,sticky,private}

## Window Focus and Swapping ##

# description: Focus or Swap with Nodes in Given Direction
super + {_,shift + }{h,j,k,l}
	bspc node -{f,s} {west,south,north,east}

# description: Focus the Next/Previous Desktop in the Current Monitor
super + {_,shift + }n
	bspc desktop -f {next,prev}.local
    notify-send "Desktop $(bspc query -D --names --desktop focused.local) selected."

# description: Focus the Older/Newer Window in the Current Desktop
super + alt + {_,shift + }n
	bspc wm -h off; \
	bspc node {older,newer} -f; \
	bspc wm -h on

# description: Focus On-- or Send Current Node to-- Desktop Number
super + {_,shift + }{1-4,8-9,0}
	bspc {desktop -f,node -d} '^{1-4,5-6,7}'; \
    notify-send "Desktop $(bspc query -D --names --desktop focused.local) selected."

## Location Preselection ##

# description: Preselect the Direction
super + ctrl + {h,j,k,l}
	bspc node -p {west,south,north,east}

# description: Preselect the Ratio
super + ctrl + {1-9}
	bspc node -o 0.{1-9}

# description: Cancel the Preselection for the Focused Node
super + u
	bspc node -p cancel

# description: Cancel the Preselection for the Focused Desktop
super + alt + u
	bspc query -N -d | xargs -I id -n 1 bspc node id -p cancel

## Move/Resize ##

# description: expand a window by moving one of its side outward
super + alt + {h,j,k,l}
	bspc node -z {left -20 0,bottom 0 20,top 0 -20,right 20 0}

# description: contract a window by moving one of its side inward
super + alt + shift + {h,j,k,l}
	bspc node -z {right -20 0,top 0 20,bottom 0 -20,left 20 0}

# description: move a floating window
super + {Left,Down,Up,Right}
	bspc node -v {-20 0,0 20,0 -20,20 0}
